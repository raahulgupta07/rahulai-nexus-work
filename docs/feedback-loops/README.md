# Feedback-loop reports

Runnable reproduce→fix→verify docs produced by the `sandbox-feedback-loop`
skill (`.agents/skills/sandbox-feedback-loop/SKILL.md`), and QA pass reports
from the `qa` skill (`qa-<date>-<scope>.md`).

Each doc is meant to be re-executable in a fresh sandbox: environment setup,
a deterministic Loop A reproduction, optional live Loop B, observed output
before and after the fix.

All reports live here (the historical root-level `sandbox-feedback-loop-*.md`
files were moved into this directory with their prefix stripped).
