# Run artifacts: logs are large (DEBUG logging writes full LLM payloads) and
# results are regenerated by run_full.sh / run_experiments.sh.
logs/
sandbox_state.json
ui/node_modules/
ui/package-lock.json
ui/package.json
